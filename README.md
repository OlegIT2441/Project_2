

# 🌱 Классификация состояния растений по мультиспектральным снимкам

### Краткое описание
Данный проект представляет собой решение задачи компьютерного зрения для точного земледелия. Основная цель — автоматическая классификация состояния сельскохозяйственных культур по снимкам, полученным с дронов (БПЛА). Система определяет, является ли растение здоровым, пораженным болезнью или испытывающим стресс (например, засуху).

---

## 🎯 Цель и мотивация

**Цель:** Разработать модель глубокого обучения, способную с высокой точностью классифицировать состояние растений на основе мультиспектральных данных.

**Мотивация:**
*   **Точное земледелие (Precision Agriculture):** Раннее выявление болезней и стрессов позволяет точечно применять агрохимикаты, снижая затраты и экологический ущерб.
*   **Масштабируемость:** Использование дронов позволяет охватывать огромные площади быстрее и детальнее, чем наземная инспекция.
*   **Мультиспектральность:** В отличие от обычных фото, мультиспектральные снимки (включая инфракрасный канал) позволяют видеть физиологические изменения в растении до того, как они станут видимы человеческому глазу.

---

## 📂 Использованные данные

В реализации проекта используется **синтетический датасет**, имитирующий мультиспектральные снимки (4 канала: R, G, B, NIR), для демонстрации работоспособности пайплайна и возможности запуска кода без скачивания гигабайтных файлов.

Однако архитектура модели полностью готова для работы с реальными данными:

1.  **PlantVillage Dataset:** Крупнейший открытый датасет изображений листьев растений (RGB).
    *   *Источник:* [Kaggle - PlantVillage Dataset](https://www.kaggle.com/datasets/emmarex/plantdisease)
2.  **AI for Agriculture Challenge:** Данные с дронов, включающие мультиспектральные снимки.
    *   *Источник:* [IEEE DataPort / Kaggle Competitions](https://www.kaggle.com/search?q=agriculture+drone+imagery)

**Структура данных (синтетических):**
*   Размерность: `4 канала x 64 x 64 пикселя`.
*   **Класс 0 (Healthy):** Высокий уровень зеленого канала, высокий NIR (высокий NDVI).
*   **Класс 1 (Diseased):** Изменения в спектре, средний уровень NIR, шумы.
*   **Класс 2 (Drought Stress):** Низкий уровень NIR, высокий уровень красного канала (пожелтение/побурение).

---

## 🧠 Архитектура модели и обоснование выбора

### Выбор модели: EfficientNet-B0
В качестве базовой архитектуры выбрана **EfficientNet-B0**.

**Обоснование:**
1.  **Эффективность:** EfficientNet обеспечивает оптимальное соотношение точности и количества параметров, что критически важно для обработки больших массивов данных с дронов и возможного развертывания на краевых устройствах (Edge AI).
2.  **Transfer Learning:** Модель предобучена на ImageNet, что позволяет использовать признаки низкого уровня (текстуры, края) для задачи классификации растений.

### Модификация для мультиспектральных данных
Стандартные CNN работают с 3-канальными изображениями (RGB). Для использования мультиспектральных данных (включая Near-Infrared) выполнена следующая модификация:
*   Первый сверточный слой (`Conv2d(3, 32, ...)`) заменен на `Conv2d(4, 32, ...)`.
*   Веса для нового (4-го) канала инициализированы средним значением весов существующих каналов, чтобы сохранить распределение активаций.

---

## 📊 Метрики качества

Для оценки качества модели используются следующие метрики:
*   **Accuracy:** Общая доля правильных ответов.
*   **F1-Score (Macro):** Усредненная мера точности и полноты, устойчивая к дисбалансу классов.
*   **Confusion Matrix:** Матрица ошибок для детального анализа того, какие классы путаются между собой.

---

## 📈 Результаты

Ниже приведены результаты обучения на синтетическом наборе данных (600 изображений, 5 эпох).

| Метрика | Значение |
| :--- | :--- |
| **Accuracy** | 1.00 (100%) |
| **Macro F1-Score** | 1.00 (100%) |

*Примечание: Высокие результаты обусловлены четким спектральным разделением классов в синтетических данных. На реальных зашумленных данных точность будет ниже, но тренд сохранится: NIR канал значительно улучшает разделение классов стресса.*

**Пример Classification Report:**

```text
                 precision    recall  f1-score   support

        Healthy       1.00      1.00      1.00        40
      Diseased       1.00      1.00      1.00        40
Drought Stress       1.00      1.00      1.00        40

      accuracy                           1.00       120
     macro avg       1.00      1.00      1.00       120
```

**Confusion Matrix (Матрица ошибок):**
Визуализация показывает идеальное разделение классов (диагональная матрица), что подтверждает корректность работы модифицированной архитектуры.

---

## 🚀 Инструкция по запуску кода

### 1. Требования (Requirements)
Для запуска проекта необходим Python 3.8+. Создайте виртуальное окружение и установите зависимости:

```text
torch>=1.9.0
torchvision>=0.10.0
scikit-learn>=0.24.0
numpy>=1.19.0
matplotlib>=3.3.0
seaborn>=0.11.0
```

Установка:
```bash
pip install -r requirements.txt
```

### 2. Запуск обучения
Код содержится в одном файле (например, `main.py`). Он включает генерацию данных, обучение и вывод метрик.

```bash
python main.py
```

### 3. Использование реальных данных
Чтобы использовать модель на реальном датасете (например, PlantVillage):
1. Скачайте датасет и распакуйте его в папку `data/`.
2. Замените класс `SyntheticMultispectralDataset` на стандартный загрузчик `torchvision.datasets.ImageFolder`.
3. Если изображения RGB, установите параметр `in_channels=3` при инициализации модели `MultispectralEfficientNet`.

---

## 📚 Список литературы / Источников

1.  **Dataset:** Hughes, D. P., & Salathé, M. (2015). *An open access repository of images on plant health to enable the development of mobile disease diagnostics*. arXiv:1511.08060. [Ссылка на PlantVillage](https://www.plantvillage.psu.edu/).
2.  **Model Architecture:** Tan, M., & Le, Q. V. (2019). *EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks*. ICML 2019. [arXiv:1905.11946](https://arxiv.org/abs/1905.11946).
3.  **Multispectral Imaging:** Mulla, D. J. (2013). *Twenty five years of remote sensing in precision agriculture: Key advances and remaining knowledge gaps*. Biosystems Engineering.
4.  **Vegetation Indices:** Rouse, J. W., et al. (1974). *Monitoring vegetation systems in the Great Plains with ERTS*. NASA Special Publication (Оригинальная работа по NDVI).
